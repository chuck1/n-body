#ifndef KERNEL_OTHER_HPP
#define KERNEL_OTHER_HPP

float			gravity(
		float m0,
		float m1,
		float r,
		float p,
		float v_p);

void		divide(unsigned int n, unsigned int * i_local0, unsigned int * i_local1);

#endif

